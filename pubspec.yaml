name: test_project
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
# Navigation package
  auto_route: ^4.0.1
# Bloc package
  flutter_bloc: ^8.0.1
# Service Locator
  injectable: ^1.5.3
  get_it: ^7.2.0
# Models generator
  freezed_annotation: ^2.0.3

dev_dependencies:
# Models generator
  freezed: ^2.0.3+1
# Navigation package generator
  auto_route_generator: ^4.0.0
  build_runner: ^2.1.11
# Tests package
  mocktail: ^0.3.0
  bloc_test: ^9.0.3
  image_test_utils: ^1.0.0
# Service Locator generator
  injectable_generator: ^1.5.3

  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true